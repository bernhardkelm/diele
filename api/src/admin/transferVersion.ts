// Bumped when the shape changes in a way an older file cannot satisfy, so an import can
// refuse rather than half-apply. Version 1 files still apply: they only lack the connectors.
export const VERSION = 2
